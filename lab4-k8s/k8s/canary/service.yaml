apiVersion: v1
kind: Service
metadata:
  name: webapp
  namespace: mzinga-lab4
  labels:
    app: webapp
    release-style: canary
spec:
  selector:
    app: webapp
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
